apiVersion: v1
kind: Secret
metadata:
  name: jev-api
  namespace: system
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
type: Opaque
# Leave data unset so applying the deployment preserves the user-patched key.
# Patch JEV_API_KEY after installation; see docs/emoji.md.
